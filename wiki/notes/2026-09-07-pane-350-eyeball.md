# Impl 79 — Pane eyeball (350px)

Source: this batch (2026-09-07). Does not edit Impl 71–78 row text. Excel task pane is the quality bar. Browser Preview is showcase-only. Do not claim DataSnipper-identical.

Excel ~350px header no longer slices language kinzi via a fixed `h-8` group. Matching still has four steps. Account OTP stays email then 6-digit code.

## Shipped

- [AppShell.tsx](../../frontend/src/features/shell/components/AppShell/AppShell.tsx) language group: `min-h-8 h-auto`. Locale buttons: `min-h-8 py-1 leading-[1.5]` (dropped `h-full`). Right cluster: `min-w-0 flex-wrap` instead of `shrink-0`. DEV / PROD badge stays.
- [styles.css](../../frontend/src/styles.css) unlayered `html[lang="my-MM"]` append: `.dt-badge` / `.dt-chip` and `[class*="text-[9px]"]` get `line-height: 1.5` and `overflow: visible`. No `padding-block`. Global and `.dt-excel-host` `overflow-x: hidden` stay.

## Known host / fail-closed gaps (open)

- Excel sideload was not run. Horizontal kinzi can still clip under `overflow-x: hidden`. Engagements `text-[10px]` was not changed.

## Not changed

- Impl 71–78 titles. CloudSessionPanel (dropdown fitted at 350). Site HTML / copy.ts. Stepper/FirstRunCue `grid-cols-4`. Nav emoji strings. ThemeToggle. Landing palette. Manifests, backend, prep-module flags. Merge to `main`.
- No commit, no push, no tag, no `CHANGELOG.md`.

Validate: Prettier, ESLint, `tsc --noEmit`, `npm test` (283 passed, including `nav-i18n`). Browser `/taskpane.html` width 350: EN then မြန်မာ; language `မြန်မာ` kinzi visible (`min-height` 32px, `line-height` 15.6px, overflow visible); nav wraps with emoji; DEV badge present; Matching four steps; Account (cloud on) still email then Sign in / Create account. Excel sideload not run.

Session (gitignored): `docs/sessions/2026-09-07-session-summary-impl-79.md`
