# Impl 100 — Public site 404 and 500 pages

Source: this batch (2026-09-08). Does not edit Impl 71–99 row text. Excel task pane is the quality bar. Browser Preview is showcase-only. Do not claim DataSnipper-identical. This is Impl 100, not 101.

Unmatched public URLs used the host default 404. Custom `404.html` and `500.html` use inner landing chrome (not the TRACE rail). English HTML fallback. Vite MPA inputs. `vercel.json` filesystem then HTTP 404 to `/404.html`. No pane ErrorBoundary.

## Shipped

- [`frontend/404.html`](../../frontend/404.html) and [`frontend/500.html`](../../frontend/500.html). Contact header/footer/dock. `site-page` main. 404 CTAs Home + Guide. 500 CTAs Home + Support.
- Copy en+my: `error404Kicker` / `error404Title` / `error404Lead` / `error404Home`, `error500Kicker` / `error500Title` / `error500Lead` / `error500Home`. [`frontend/site/site.ts`](../../frontend/site/site.ts) titles for `data-page="not-found"` and `data-page="server-error"`.
- [`vite.config.ts`](../../vite.config.ts) inputs `notFound` and `serverError`. [`vercel.json`](../../vercel.json): `cleanUrls: false`, filesystem, then `status: 404` `dest: /404.html`. No catch-all 500 route. No rewrite of `/taskpane.html`.
- Tests in [`frontend/src/test/site-pages-i18n.test.ts`](../../frontend/src/test/site-pages-i18n.test.ts).

## Known host / fail-closed gaps (open)

- Vite dev does not apply `vercel.json`. Unmatched-path HTTP 404 is Vercel-owned. Excel sideload was not run. Contact mailbox is not live.

## Not changed

- Impl 71–99 titles. Landing hero/TRACE. Pane `taskpane.html` / `AppLayout` / `PaneSkeleton`. Matching, OCR, persist, R2, Brevo. Impl 101 ErrorBoundary. Cookie page. No commit until asked. No `development` merge into `main`. No tag. No `CHANGELOG.md`.

Validate: Prettier, ESLint, `tsc --noEmit`, `npm test`. Browser `/404.html` and `/500.html`; language toggle; `/` and `/taskpane.html` unchanged.

Session (gitignored): `docs/sessions/2026-09-08-session-summary-impl-100.md`
