# Impl 19 — Lark Task handoff rule

Source: this batch (2026-08-31). Same day as Impl 18, so the session file uses `-impl-19`.

Recorded the required handoff: after each meaningful batch, wiki Impl row + wiki note + gitignored session **and** a Lark Task in the chat reply.

- Root `AGENTS.md` Quality gates now require all four. Handoff output section has the exact fenced copy-block shape.
- `wiki/conventions/handoff.md` is the wiki pointer. Lark Task stays chat-only; do not commit the copy block into wiki notes.
- App code, matching, Excel, OCR unchanged.

Session (gitignored): `docs/sessions/2026-08-31-session-summary-impl-19.md`
