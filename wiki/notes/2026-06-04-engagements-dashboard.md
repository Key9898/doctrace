# Impl 14 — Audit engagements dashboard

Source: git `6e73675` (2026-06-04). Not a standalone CHANGELOG or last_session heading.

- Added `EngagementManager` and `activeModule` switch (engagements vs matching)
- Task pane can manage audit engagements separately from the matching workflow
- Same-day git `4e4b45c` snipping/WebView2 polish is recorded under Impl 13, not here

Session (gitignored): `docs/sessions/2026-06-04-session-summary.md`
