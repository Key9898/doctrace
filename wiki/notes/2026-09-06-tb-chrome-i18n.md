# Impl 65 — Trial Balance chrome i18n

Source: this batch (2026-09-06). Same leftover family as Impl 46–64; session file uses `-impl-65`. Excel task pane is the quality bar. Browser Preview is showcase-only. Do not claim DataSnipper-identical.

Trial Balance UI chrome follows locale. Parse, mapping, ticks, and Send stay as Impl 63. Matching stays unblocked. Listing files do not enter Matching Import.

## Shipped

- Remaining TB chrome in `frontend/src/lib/i18n/translations.ts` (`tb.kicker` through `tb.guidelinesTitle`) for `en-US` and `my-MM`. Impl 63 intake keys unchanged.
- `TrialBalance.tsx` uses `t(...)`. Kicker/title mojibake and the `locale === "my-MM"` ternary are gone.
- Balance helper is visible only: mismatch does not claim Send is blocked. Guidelines heading is sample-to-Matching, not a fake rollover product. Mock account names and F/S group strings stay English lead keys.
- Vitest: `tb-i18n.test.ts`. No RTL. No Prisma.
- Prep remaining: TB chrome i18n moved to Scaffold. Open is Workpapers and Client PBC remaining mock copy. Last numbered Impl 65.

## Known host / fail-closed gaps (open)

- Excel sideload was not run. Leftover B is still 502/503.

## Not changed

- SheetJS parse, lead mapping, ticks, Send, `tb-selection` snapshot headers, AppShell, Workpapers, ClientPortal, manifests, `frontend/site/`, leftover B keys, leftover C, `VITE_SHOW_PREP_MODULES` on this machine.
- Workpapers mock i18n. Client PBC mock i18n. Minutes link. Dashboard counts. ISA 505/580. CaseWare OS.
- No commit, no push, no merge, no tag, no `CHANGELOG.md`.

Validate: Prettier, ESLint, `tsc --noEmit`, `npm test` (237 passed, including 4 `tb-i18n`). Browser `/taskpane.html`: Trial Balance EN and `my-MM` chrome readable (no mojibake); F/S groups still English; Import still `invoice-01.json` only (no listing); `/` stays landing. Listing file picker is not available in browser automation, so Send was not re-clicked this slice; `tb-intake` still 8 passed.

Session (gitignored): `docs/sessions/2026-09-06-session-summary-impl-65.md`
