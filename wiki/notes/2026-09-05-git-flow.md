# Impl 45 — Git branch rules in AGENTS.md

Source: this batch (2026-09-05). Same-day family as Impl 43–44; session file uses `-impl-45`. Excel task pane is the quality bar. Browser Preview is showcase-only. Do not claim DataSnipper-identical.

## Shipped

- Root `AGENTS.md` now has a Git branches section: daily work on `development`, client drop on `main`, no GitHub PR, never merge the `development` tip while prep-module commit `d7f47c6` is on it, never name branches `phase-N`, no tag until a client drop, Vercel production must deploy `main`.
- The same Git branches section is on `main` at `0baa373` (`AGENTS.md` only; parent `cac3878`). This Impl pushes that commit to `origin/main`. `d7f47c6` and prep-module files stay off `main`.
- `wiki/architecture/overview.md` tree lists `trial-balance`, `workpapers`, and `pbc-portal` as `development`-only mocks behind `VITE_SHOW_PREP_MODULES`. That overview edit stays on `development` (those folders are not on `main`).

## Known host / fail-closed gaps (open)

- Excel sideload was not run in this Impl.
- Vercel Production follows `origin/main`. After this Impl that tip is `0baa373` (AGENTS.md only on `cac3878`). Preview exists for `d7f47c6`. Do not set `VITE_SHOW_PREP_MODULES` on Vercel.

## Not changed

- README, wiki conventions, product plan, `.env.example`, app code, manifests, `VITE_API_URL`, backend, keys, migrate.
- No merge of `development` into `main`. No PR. No tag. No `CHANGELOG.md`.

Validate: Prettier on touched markdown. No app code change; ESLint/tsc/tests not required for this batch.

Session (gitignored): `docs/sessions/2026-09-05-session-summary-impl-45.md`
