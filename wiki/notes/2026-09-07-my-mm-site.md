# Impl 73 — public site glossary and MY typography

Source: this batch (2026-09-07). Excel task pane is the quality bar. Browser Preview is showcase-only. Do not claim DataSnipper-identical.

Public-site `my` copy keeps the same product terms as pane Impl 72. `html[lang="my"]` no longer applies Latin uppercase or letter-spacing to Myanmar chrome. Site default locale stays English.

## Shipped

- `frontend/site/site.css`: Myanmar system font fallback after Outfit. Unlayered `html[lang="my"]` resets for Tailwind `uppercase` and tracking utilities. No Myanmar webfont download.
- `copy.ts` `my`: expanded Privacy nav; clearer skip/hero/does/not/OTP hint. Kickers, Trial Balance, Profile, URLs, and `copy.en` unchanged.
- Vitest: `site-glossary-i18n.test.ts`. `site-profile-i18n.test.ts` left as-is (`navProfile` EN is `Profile`).
- Phases row 73 sits between 72 and 75. Last numbered Impl is 76. Not a prep-module remaining item.

## Known host / fail-closed gaps (open)

- Excel sideload was not run. Leftover B is still 502/503. `/auth-code.html` still redirects to sign-in without a stored OTP challenge; hint copy is unit-tested only.

## Not changed

- `site.ts` default `en`, OTP, Profile menu. Off-white paper/wash. Pane `translations.ts` / `styles.css`. Impl 74 toasts and activity. Engine/CSV/audit English. Rail `TRACE · MATCH · SNIP`.
- No commit, no push, no merge, no tag, no `CHANGELOG.md`.

Validate: Prettier, ESLint, `tsc --noEmit`, `npm test` (276 passed). Browser `/`: EN first (or toggle); မြန်မာ nav `ကိုယ်ရေးမူဝါဒ`; kicker `Excel task pane · Test of Details`; heroLead full sentence; section labels not Latin-uppercase. Support / privacy / terms spotted. Profile menu still opens.

Session (gitignored): `docs/sessions/2026-09-07-session-summary-impl-73.md`
