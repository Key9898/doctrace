# Impl 66 — Workpapers chrome i18n

Source: this batch (2026-09-06). Same leftover family as Impl 46–65; session file uses `-impl-66`. Excel task pane is the quality bar. Browser Preview is showcase-only. Do not claim DataSnipper-identical.

Workpapers UI chrome follows locale. Sign gate and pack snapshot stay as Impl 64. Matching stays unblocked.

## Shipped

- Remaining Workpapers chrome in `frontend/src/lib/i18n/translations.ts` (`wp.kicker` through `wp.noteClosed`) for `en-US` and `my-MM`. Impl 64 pack keys unchanged.
- `Workpapers.tsx` uses `t(...)`. Kicker/title mojibake and the `locale === "my-MM"` ternaries are gone. Mock header reuses `titleBlock` plus subtitle.
- Status badges are display-only maps. Stored mock unions, workpaper names, authors, and note bodies stay English. Progress inline width unchanged.
- Vitest: `wp-i18n.test.ts`. No RTL. No Prisma.
- Prep remaining: Workpapers chrome i18n moved to Scaffold. Open is Client PBC remaining mock copy. Last numbered Impl 66.

## Known host / fail-closed gaps (open)

- Excel sideload was not run. Leftover B is still 502/503.

## Not changed

- `wp-pack`, `wp-signoff`, Send from Review, Zustand pack, ResultsPanel, Trial Balance, ClientPortal, AppShell, manifests, `frontend/site/`, leftover B keys, leftover C, `VITE_SHOW_PREP_MODULES` on this machine.
- Client PBC mock i18n. Minutes link. Dashboard counts. ISA 505/580. CaseWare OS.
- No commit, no push, no merge, no tag, no `CHANGELOG.md`.

Validate: Prettier, ESLint, `tsc --noEmit`, `npm test` (240 passed, including 3 `wp-i18n`). Browser `/taskpane.html`: Workpapers pack branch EN and `my-MM` kicker/title readable (no mojibake); Sign disabled while exception unsigned; Import still `invoice-01.json`; `/` stays landing. Mock list/notes were hidden by the existing pack; those keys are covered by `wp-i18n.test.ts`.

Session (gitignored): `docs/sessions/2026-09-06-session-summary-impl-66.md`
